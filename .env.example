# Copy this file to run/.env and fill in your own key.
# The key is used only by the GPT-4o rewriting module (demo apps)
# and by experiments/gpt4o_ablation_generate.py.
OPENAI_API_KEY=sk-your-key-here
